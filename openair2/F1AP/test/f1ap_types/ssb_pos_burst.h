#ifndef SSB_POS_BURST_MIR_H
#define  SSB_POS_BURST_MIR_H

#include <stdbool.h>

typedef struct{

} ssb_pos_burst_t ;

void free_ssb_pos_burst(ssb_pos_burst_t* src);

bool eq_ssb_pos_burst(ssb_pos_burst_t const* m0, ssb_pos_burst_t const* m1 );

#endif

